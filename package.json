{
  "name": "vinyl-archive",
  "version": "0.1.0",
  "private": true,
  "description": "Dev orchestration only — runs the backend and frontend dev servers together with `npm run dev`. See backend/ and frontend/ for the actual apps, and DEPLOYMENT.md for self-hosting.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend"
  }
}
